# 既定の規則に加えて、この版で守りたいものを足す。
#
# 規則の名前は実際に発火する文言と正確に一致させる。hlint は知らない名前を
# 黙って無視するので、書いただけでは効いている証拠にならない（hlint の
# --default が出す一覧のコメントは "Use explicit module export list" だが、
# 実際に発火する名前は "Use module export list" である）。
- warn: {name: Use module export list}
