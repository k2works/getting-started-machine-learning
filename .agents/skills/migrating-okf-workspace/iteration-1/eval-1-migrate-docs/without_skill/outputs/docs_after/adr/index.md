# ADR (Architecture Decision Records)

技術的意思決定を記録した ADR の一覧です。ADR の作成には `creating-adr` スキルを使用してください。
各 ADR はフロントマターに `type: Architecture Decision Record` と `title`・`description`・`status`（draft / stable / deprecated）を持つコンセプトとして本ディレクトリに置き、作成時に本一覧へ追記します。

# ADR 一覧

* （まだ ADR はありません）
